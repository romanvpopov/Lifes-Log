<UserControl
    x:Class="LL.LLEvents.EventFilter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LL.LLEvents"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400" Loaded="UserControl_Loaded">
    <RelativePanel >
        <Button x:Name="Period"
                Click="Period_Click"
                RelativePanel.AlignTopWithPanel="True"/>
        <ListView x:Name="EventTypeList"
                  VerticalAlignment="Stretch"
                  SelectionMode="Multiple"
                  IsMultiSelectCheckBoxEnabled="False"
                  RelativePanel.Below="Period" />
        <TextBox x:Name="AS"
                 Text="Search"
                 RelativePanel.Below="EventTypeList"/>
        <StackPanel Orientation="Horizontal" RelativePanel.Below="AS">
                <Button x:Uid="BTApply" Click="Refresh_Click"/>
                <Button x:Name="BTReset" x:Uid="BTReset" Click="Reset_Click" Visibility="Collapsed"/>
        </StackPanel>
    </RelativePanel>
</UserControl>
