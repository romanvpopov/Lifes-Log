<UserControl
    x:Class="LL.Healths.HQValue"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border x:Name="BL"
            BorderBrush="{x:Null}"
            Background="{ThemeResource ButtonBorderThemeBrush}">
        <TextBlock x:Name="TX"
                   TextAlignment="Right"
                   HorizontalAlignment="Stretch"/>
    </Border>
</UserControl>
